```sh
# nodejs version "^18.18.0 || ^19.8.0 || >= 20.0.0" is required.

# install
pnpm i

# terminal 1 — Next.js
pnpm dev                         # → localhost:3001

# terminal 2 — expresia-api server
cd expresia-api && pnpm dev      # → localhost:3002

# build
pnpm build
```